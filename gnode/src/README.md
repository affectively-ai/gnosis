# gnode Source

Parent: [gnode README](../README.md)

This directory contains the Rust CLI entrypoint for `gnode`.

- `main.rs`: command parsing, Bun bridge-driver delegation, passthrough stdout/stderr, and the `run` / `compile` / `schedule` command surface.
